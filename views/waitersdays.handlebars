<!DOCTYPE html>

<head>
  <html>

  <body>
    <output><kbd>
      {{#if message.error}}
      <div class="error">
        {{message.rror}}
      </div>
      {{/if}}
      <br>
      </br>

      {{#if messages.success}}
      <div class="success">
        {{messages.success}}
      </div>
      {{/if}}

      <p>Hello, {{name}}, select your prefered working days below.</p>
    </output></kbd>
    <form class="days" action="" method="post">
      <input type="checkbox" name="day" value="Monday" {{#if days.Monday}} checked {{/if}}> Monday
      <input type="checkbox" name="day" value="Tuesday" {{#if days.Tuesday}} checked {{/if}}> Tuesday
      <input type="checkbox" name="day" value="Wednesday" {{#if days.Wednesday}} checked {{/if}}> Wednesday
      <input type="checkbox" name="day" value="Thursday" {{#if days.Thursday}} checked {{/if}}> Thursday
      <input type="checkbox" name="day" value="Friday" {{#if days.Friday}} checked {{/if}}> Friday
      <input type="checkbox" name="day" value="Saturday" {{#if days.Saturday}} checked {{/if}}> Saturday
      <input type="checkbox" name="day" value="Sunday" {{#if days.Sunday}} checked {{/if}}> Sunday
      <br><input type="submit" name="submit" value="Submit" required>
    </form>
    <hr>
</body>
   <!-- {{daysToWork}} -->
  </html>
